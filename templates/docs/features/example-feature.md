# Example Feature

Extended description goes here. This file is optional — only needed if the feature brief
in `example-feature.yaml` needs more detail.

## Background

Context for why this feature is needed.

## User Story

As a [role], I want [capability], so that [benefit].

## Technical Notes

Any implementation hints, constraints, or references.
