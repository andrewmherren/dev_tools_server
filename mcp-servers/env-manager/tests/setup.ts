// Vitest setup file for env-manager tests
// Add any global test setup here

import { beforeAll, afterAll } from 'vitest';

beforeAll(() => {
    // Setup test environment
});

afterAll(() => {
    // Cleanup after tests
});
